# 更新日志

所有显著变更都会记录在本文件中。

## v0.2.1 — 2026-09-01

### 修复

- **弹窗遮罩不可见**：`.page-stagger` 的分段入场规则误伤固定定位的弹窗遮罩，导致遮罩永久透明且整体下移 14px。已为 `.modal-mask` 增加豁免
- **弹层没有退场动画**：弹窗 / 命令面板 / 右键菜单 / 筛选浮层关闭时瞬间消失。新增统一的 `overlay-out` 退场过渡，遮罩淡出并让面板沿入场方向回落
- **Toast 没有退场动画**：改用 `TransitionGroup` 驱动进场、退场与补位，多个 Toast 堆叠时剩余项平滑上移
- **「减弱动态效果」下内容闪现**：只重置了动画时长却未重置延迟，分段入场元素会先隐形最长 0.22s 再突然出现。现已一并清零延迟
- **平级页面切换方向错乱**：导航层级相同时误判为「返回」而向左滑，现改为柔和上浮
- **删除按钮无法用键盘找到**：Todo 的删除按钮仅在鼠标悬停时显示，现补上键盘聚焦态

### 其他

- 页面切换的全页模糊由 5px 降到 3px，降低 GPU 开销，景深感保留
- 清理动画层中被组件内样式覆盖的失效规则

## v0.2.0 — 2026-08-30

### 新增

- **首次启动初始引导**：四步向导设置称呼、性别与工作，完成后首页问候语带上称呼；设置页新增「个人资料」区可随时修改，并可重新运行引导
- **问候语时段化**：早上好 / 下午好 / 晚上好，随时间自动变化
- **过渡动画**：页面切换淡入淡出、卡片交错入场、卡片悬停浮起、右键菜单 / 弹层 / 命令面板入场动效，并适配系统「减弱动态效果」偏好

### 修复

- 页面切换抖动：滚动条槽位固定（`scrollbar-gutter: stable`）+ 新页面入场时重置滚动位置

### 其他

- 关于页版本号改为运行时读取，不再硬编码

## v0.1.0 — 2026-08-29

首个公开发行版（原 Project Hub，现更名 **FatFish**）：

- 本地扫描导入项目，自动识别 Java / JS / TS / Python / Rust / Go 等技术栈
- 五态项目状态：计划中 / 进行中 / 已暂停 / 已完成 / 已归档，卡片菜单一键切换
- 集合（批量导入）、彩色标签、多维筛选与排序
- Todo 待办（关联项目 / 截止日期）、多类型链接、备注、活动时间线
- Git 只读信息、Ctrl+K 命令面板、浅色 / 深色主题、中英双语
- 数据全部存本机 SQLite（`%APPDATA%/com.fatfish.app/fatfish.db`）
